const initialState = {
  // Student Union.
  initialRegion: {
    latitude: 28.601660,
    longitude: -81.200788,
    latitudeDelta: 0.014,
    longitudeDelta: 0.014
  }
};

const map = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default map;
